services:
  app:
    build: .
    ports: ["3000:3000"]
    env_file: .env
    volumes:
      - ./data:/app/data
      - ./sessions:/app/sessions
      - ./screenshots:/app/screenshots
      - ./logs:/app/logs
    restart: unless-stopped
